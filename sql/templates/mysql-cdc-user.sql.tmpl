-- ============================================================
-- Usuario CDC para Debezium (solo lectura + replicacion)
-- y usuario de aplicacion para pruebas de escritura.
-- Template: {{VAR}} es sustituido por scripts/gen-secrets.ps1
-- ============================================================

CREATE USER IF NOT EXISTS '{{MYSQL_CDC_USER}}'@'%' IDENTIFIED BY '{{MYSQL_CDC_PASSWORD}}';
ALTER USER '{{MYSQL_CDC_USER}}'@'%' IDENTIFIED BY '{{MYSQL_CDC_PASSWORD}}';

GRANT SELECT, SHOW DATABASES, SHOW VIEW, RELOAD,
      REPLICATION SLAVE, REPLICATION CLIENT, LOCK TABLES, PROCESS
  ON *.* TO '{{MYSQL_CDC_USER}}'@'%';

CREATE USER IF NOT EXISTS '{{MYSQL_APP_USER}}'@'%' IDENTIFIED BY '{{MYSQL_APP_PASSWORD}}';
ALTER USER '{{MYSQL_APP_USER}}'@'%' IDENTIFIED BY '{{MYSQL_APP_PASSWORD}}';

GRANT ALL PRIVILEGES ON `{{CDC_SOURCE_DB}}`.* TO '{{MYSQL_APP_USER}}'@'%';

FLUSH PRIVILEGES;
